# tkinter is part of the Python standard library (not pip-installable).
# Requires Python 3.12 with Tk 8.6. On Debian/Ubuntu install via: sudo apt install python3-tk
openpyxl==3.1.5
pandas==3.0.3
pillow==12.2.0
python-barcode==0.16.1
python-docx==1.2.0
pyinstaller==6.20.0
